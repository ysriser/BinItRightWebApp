version: '3.8'
services:
  db:
    ports:
      - "0.0.0.0:3306:3306"
    restart: always
    volumes:
      - mysql_prod_data:/var/lib/mysql # Persistent storage

  java-app:
    restart: always

volumes:
  mysql_prod_data:
